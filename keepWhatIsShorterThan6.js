// Dado um vetor de strings, filtre os elementos do vetor para conter apenas strings com 5 caractéres ou menos.

const fruits = ['tomate', 'limão', 'maçã', 'abacate', 'morango'];

const fruitsWith5CharsOrLess = fruits.filter((fruit) => fruit.length <= 5);